{
  "name": "spice",
  "version": "1.1.0",
  "description": "Agent toolkit for LLM-assisted development projects",
  "python_requires": ">=3.10",

  "_note": "Components are discovered from the filesystem and described by their own frontmatter: version, tier, phase, category, suggested. They are deliberately not listed here — a second copy drifts from the first and nothing detects it. Use 'spice list --available'."
}
